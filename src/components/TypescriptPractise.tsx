const TypescriptPractise = () => {
  //     Type a function getFullName correctly
  // – Q1: Write a function getFullName which gets name and surname and
  // returns a full name

  function getFullName() {}

  return <div></div>;
};

export default TypescriptPractise;
